import { StyleSheet } from "react-native";

const styles = StyleSheet.create({
    container: {
        display: 'flex',
        alignItems: 'center',
        flex: 1,
        textAlign: 'center',
        width: '100%',
        overflow: 'hidden',
        marginTop: 24
    }
})

export default styles;